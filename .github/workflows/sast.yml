name: ESLint for SAST

on:
  push:
    branches:
      - main

jobs:
  eslint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install ESLint dependencies
        run: npm install

      - name: Run ESLint
        run: eslint --fix-dry-run . --ext .js,.jsx,.ts,.tsx

      - name: Display ESLint results
        run: echo "ESLint suggestions:" && cat eslint-output.txt
